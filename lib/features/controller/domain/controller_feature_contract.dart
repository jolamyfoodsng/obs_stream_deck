class ControllerFeatureContract {
  const ControllerFeatureContract._();

  static const String description =
      'Controller feature manages stream deck pages, button grids, and runtime action execution.';
}
