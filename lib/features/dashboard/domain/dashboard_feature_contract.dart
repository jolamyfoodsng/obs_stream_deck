class DashboardFeatureContract {
  const DashboardFeatureContract._();

  static const String description =
      'Dashboard feature shows compact runtime metrics from OBS streams and health states.';
}
