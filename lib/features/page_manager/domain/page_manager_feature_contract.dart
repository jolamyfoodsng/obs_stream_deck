class PageManagerFeatureContract {
  const PageManagerFeatureContract._();

  static const String description =
      'Page manager handles CRUD operations for controller pages and default ordering.';
}
